# Canonical YY.M.D.B project version, stamped into binaries at
# compile time (refineid_lib_core::VERSION, the CLI user agent).
# Kept equal to the VERSION file at the repo root.
[env]
REFINEID_VERSION = { value = "26.9.24.210", force = true }
